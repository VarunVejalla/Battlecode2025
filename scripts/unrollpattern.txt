
            if (nearbyMapInfos[0] == null || nearbyMapInfos[0].hasRuin() || nearbyMapInfos[0].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0x5DFDDF5FDDDFDDFFL;
            } else {
                PaintType paint = nearbyMapInfos[0].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0x5DFDDF5FDDDFDDFFL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0xA20220A022202200L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0x7DFFDFFFDFDFFDFFL;
                        break;
                    default:
                        validBitstring &= 0xDFFDFF5FFDFFDFFFL;
                        break;
                }
            }


            if (nearbyMapInfos[1] == null || nearbyMapInfos[1].hasRuin() || nearbyMapInfos[1].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xDDF9DF5F9DDF9DFFL;
            } else {
                PaintType paint = nearbyMapInfos[1].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xDDF9DF5F9DDF9DFFL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x220620A062206200L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFFBFF5FFDFF9FFFL;
                        break;
                    default:
                        validBitstring &= 0xDDFDDFFF9FDFFDFFL;
                        break;
                }
            }


            if (nearbyMapInfos[2] == null || nearbyMapInfos[2].hasRuin() || nearbyMapInfos[2].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0x9FF9DF9F9D9F9DFFL;
            } else {
                PaintType paint = nearbyMapInfos[2].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0x9FF9DF9F9D9F9DFFL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x6006206062606200L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0x9FFFFFFF9F9FFDFFL;
                        break;
                    default:
                        validBitstring &= 0xFFF9DF9FFDFF9FFFL;
                        break;
                }
            }


            if (nearbyMapInfos[3] == null || nearbyMapInfos[3].hasRuin() || nearbyMapInfos[3].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xBBF9DEBF9DBF9DFFL;
            } else {
                PaintType paint = nearbyMapInfos[3].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xBBF9DEBF9DBF9DFFL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x4406214062406200L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFFDFEBFFDFF9FFFL;
                        break;
                    default:
                        validBitstring &= 0xBBFBDFFF9FBFFDFFL;
                        break;
                }
            }


            if (nearbyMapInfos[4] == null || nearbyMapInfos[4].hasRuin() || nearbyMapInfos[4].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xBBFBDEBFBDBFBDFFL;
            } else {
                PaintType paint = nearbyMapInfos[4].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xBBFBDEBFBDBFBDFFL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x4404214042404200L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFBFFDFFFBFBFFDFFL;
                        break;
                    default:
                        validBitstring &= 0xBFFBFEBFFDFFBFFFL;
                        break;
                }
            }


            if (nearbyMapInfos[5] == null || nearbyMapInfos[5].hasRuin() || nearbyMapInfos[5].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0x5DF5FF5F5FDF5FDFL;
            } else {
                PaintType paint = nearbyMapInfos[5].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0x5DF5FF5F5FDF5FDFL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0xA20A00A0A020A020L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0x5FFFFFFF5FDFFFDFL;
                        break;
                    default:
                        validBitstring &= 0xFDF5FF5FFFFF5FFFL;
                        break;
                }
            }


            if (nearbyMapInfos[6] == null || nearbyMapInfos[6].hasRuin() || nearbyMapInfos[6].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0x5DF5DF5F5DDF5DDDL;
            } else {
                PaintType paint = nearbyMapInfos[6].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0x5DF5DF5F5DDF5DDDL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0xA20A20A0A220A222L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFF5FF7FFDFF5FFDL;
                        break;
                    default:
                        validBitstring &= 0x5DFFDFDF5FDFFDDFL;
                        break;
                }
            }


            if (nearbyMapInfos[7] == null || nearbyMapInfos[7].hasRuin() || nearbyMapInfos[7].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xDDF9DF5F1DDF1D9DL;
            } else {
                PaintType paint = nearbyMapInfos[7].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xDDF9DF5F1DDF1D9DL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x220620A0E220E262L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xDDFFDFFF3FDFFD9FL;
                        break;
                    default:
                        validBitstring &= 0xFFF9FF5FDDFF1FFDL;
                        break;
                }
            }


            if (nearbyMapInfos[8] == null || nearbyMapInfos[8].hasRuin() || nearbyMapInfos[8].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0x9FF9DF9F9D9E1D9DL;
            } else {
                PaintType paint = nearbyMapInfos[8].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0x9FF9DF9F9D9E1D9DL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x600620606261E262L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFF9FF9FFFFE1FFDL;
                        break;
                    default:
                        validBitstring &= 0x9FFFDFFF9D9FFD9FL;
                        break;
                }
            }


            if (nearbyMapInfos[9] == null || nearbyMapInfos[9].hasRuin() || nearbyMapInfos[9].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xBBF9DEBE9DBE9D9DL;
            } else {
                PaintType paint = nearbyMapInfos[9].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xBBF9DEBE9DBE9D9DL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x4406214162416262L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xBBFFDFFEDFBFFD9FL;
                        break;
                    default:
                        validBitstring &= 0xFFF9FEBFBDFE9FFDL;
                        break;
                }
            }


            if (nearbyMapInfos[10] == null || nearbyMapInfos[10].hasRuin() || nearbyMapInfos[10].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xBBEBDEBEBDBEBDBDL;
            } else {
                PaintType paint = nearbyMapInfos[10].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xBBEBDEBEBDBEBDBDL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x4414214142414242L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFEBFEFFFDFEBFFDL;
                        break;
                    default:
                        validBitstring &= 0xBBFFDFBEBFBFFDBFL;
                        break;
                }
            }


            if (nearbyMapInfos[11] == null || nearbyMapInfos[11].hasRuin() || nearbyMapInfos[11].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xBBEBFEBEBFBEBFBFL;
            } else {
                PaintType paint = nearbyMapInfos[11].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xBBEBFEBEBFBEBFBFL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x4414014140414040L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xBFFFFFFEBFBFFFBFL;
                        break;
                    default:
                        validBitstring &= 0xFBEBFEBFFFFEBFFFL;
                        break;
                }
            }


            if (nearbyMapInfos[12] == null || nearbyMapInfos[12].hasRuin() || nearbyMapInfos[12].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0x5DF7BF5F7BDF7BFFL;
            } else {
                PaintType paint = nearbyMapInfos[12].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0x5DF7BF5F7BDF7BFFL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0xA20840A084208400L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xDDFFBFFF7FDFFBFFL;
                        break;
                    default:
                        validBitstring &= 0x7FF7FF5FFBFF7FFFL;
                        break;
                }
            }


            if (nearbyMapInfos[13] == null || nearbyMapInfos[13].hasRuin() || nearbyMapInfos[13].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0x5DF5BF5F5BDF5BDBL;
            } else {
                PaintType paint = nearbyMapInfos[13].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0x5DF5BF5F5BDF5BDBL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0xA20A40A0A420A424L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFF5FFDFFBFF5FFBL;
                        break;
                    default:
                        validBitstring &= 0x5DFFBF7F5FDFFBDFL;
                        break;
                }
            }


            if (nearbyMapInfos[14] == null || nearbyMapInfos[14].hasRuin() || nearbyMapInfos[14].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0x5DF59F5F59DF59D8L;
            } else {
                PaintType paint = nearbyMapInfos[14].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0x5DF59F5F59DF59D8L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0xA20A60A0A620A627L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0x5DFF9FFF5FFFF9DEL;
                        break;
                    default:
                        validBitstring &= 0xFFF5FF5FF9DF5FF9L;
                        break;
                }
            }


            if (nearbyMapInfos[15] == null || nearbyMapInfos[15].hasRuin() || nearbyMapInfos[15].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xDDF9DF5F19DF1990L;
            } else {
                PaintType paint = nearbyMapInfos[15].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xDDF9DF5F19DF1990L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x220620A0E620E66FL;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFF9FF5FF9FF3FF1L;
                        break;
                    default:
                        validBitstring &= 0xDDFFDFFF1FDFD99EL;
                        break;
                }
            }


            if (nearbyMapInfos[16] == null || nearbyMapInfos[16].hasRuin() || nearbyMapInfos[16].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0x9FF9DF9F9D9E1190L;
            } else {
                PaintType paint = nearbyMapInfos[16].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0x9FF9DF9F9D9E1190L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x600620606261EE6FL;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0x9FFFDFFF9F9FF39EL;
                        break;
                    default:
                        validBitstring &= 0xFFF9FF9FFDFE1DF1L;
                        break;
                }
            }


            if (nearbyMapInfos[17] == null || nearbyMapInfos[17].hasRuin() || nearbyMapInfos[17].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xBBF9DEBE95BE9590L;
            } else {
                PaintType paint = nearbyMapInfos[17].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xBBF9DEBE95BE9590L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x440621416A416A6FL;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFF9FEBFF5FEDFF1L;
                        break;
                    default:
                        validBitstring &= 0xBBFFDFFE9FBFB59EL;
                        break;
                }
            }


            if (nearbyMapInfos[18] == null || nearbyMapInfos[18].hasRuin() || nearbyMapInfos[18].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xBBEB5EBEB5BEB5B4L;
            } else {
                PaintType paint = nearbyMapInfos[18].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xBBEB5EBEB5BEB5B4L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x4414A1414A414A4BL;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xBBFF5FFEBFFFF5BEL;
                        break;
                    default:
                        validBitstring &= 0xFFEBFEBFF5BEBFF5L;
                        break;
                }
            }


            if (nearbyMapInfos[19] == null || nearbyMapInfos[19].hasRuin() || nearbyMapInfos[19].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xBBEB7EBEB7BEB7B7L;
            } else {
                PaintType paint = nearbyMapInfos[19].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xBBEB7EBEB7BEB7B7L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x4414814148414848L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFEBFFBFF7FEBFF7L;
                        break;
                    default:
                        validBitstring &= 0xBBFF7EFEBFBFF7BFL;
                        break;
                }
            }


            if (nearbyMapInfos[20] == null || nearbyMapInfos[20].hasRuin() || nearbyMapInfos[20].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xBBEF7EBEF7BEF7FFL;
            } else {
                PaintType paint = nearbyMapInfos[20].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xBBEF7EBEF7BEF7FFL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x4410814108410800L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xBBFF7FFEFFBFF7FFL;
                        break;
                    default:
                        validBitstring &= 0xFFEFFEBFF7FEFFFFL;
                        break;
                }
            }


            if (nearbyMapInfos[21] == null || nearbyMapInfos[21].hasRuin() || nearbyMapInfos[21].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0x7DD7BF5D7BDD7BFFL;
            } else {
                PaintType paint = nearbyMapInfos[21].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0x7DD7BF5D7BDD7BFFL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x822840A284228400L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFDFFF5FFBFD7FFFL;
                        break;
                    default:
                        validBitstring &= 0x7DF7BFFD7FDFFBFFL;
                        break;
                }
            }


            if (nearbyMapInfos[22] == null || nearbyMapInfos[22].hasRuin() || nearbyMapInfos[22].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0x7DD7BF5D5BDD5B5BL;
            } else {
                PaintType paint = nearbyMapInfos[22].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0x7DD7BF5D5BDD5B5BL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x822840A2A422A4A4L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0x7DFFBFFDDFDFFB5FL;
                        break;
                    default:
                        validBitstring &= 0xFFD7FF5F7BFD5FFBL;
                        break;
                }
            }


            if (nearbyMapInfos[23] == null || nearbyMapInfos[23].hasRuin() || nearbyMapInfos[23].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0x7DD7BF5D59DD5948L;
            } else {
                PaintType paint = nearbyMapInfos[23].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0x7DD7BF5D59DD5948L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x822840A2A622A6B7L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFD7FF5FF9FDDFE9L;
                        break;
                    default:
                        validBitstring &= 0x7DFFBFFD5FDF795EL;
                        break;
                }
            }


            if (nearbyMapInfos[24] == null || nearbyMapInfos[24].hasRuin() || nearbyMapInfos[24].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xFDFFFF5D19DD1800L;
            } else {
                PaintType paint = nearbyMapInfos[24].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xFDFFFF5D19DD1800L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x20000A2E622E7FFL;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFDFFFFFD1FDFF83EL;
                        break;
                    default:
                        validBitstring &= 0xFFFFFF5FF9FD1FC1L;
                        break;
                }
            }


            if (nearbyMapInfos[25] == null || nearbyMapInfos[25].hasRuin() || nearbyMapInfos[25].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xFFFFFF9F9D981000L;
            } else {
                PaintType paint = nearbyMapInfos[25].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xFFFFFF9F9D981000L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x606267EFFFL;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFFFFF9FFDF81FE3L;
                        break;
                    default:
                        validBitstring &= 0xFFFFFFFF9F9FF01CL;
                        break;
                }
            }


            if (nearbyMapInfos[26] == null || nearbyMapInfos[26].hasRuin() || nearbyMapInfos[26].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xFBFFFEBA95BA9400L;
            } else {
                PaintType paint = nearbyMapInfos[26].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xFBFFFEBA95BA9400L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x40001456A456BFFL;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFBFFFFFA9FBFF45EL;
                        break;
                    default:
                        validBitstring &= 0xFFFFFEBFF5FA9FA1L;
                        break;
                }
            }


            if (nearbyMapInfos[27] == null || nearbyMapInfos[27].hasRuin() || nearbyMapInfos[27].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xFBAF7EBAB5BAB4A4L;
            } else {
                PaintType paint = nearbyMapInfos[27].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xFBAF7EBAB5BAB4A4L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x45081454A454B5BL;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFAFFEBFF5FBBFE5L;
                        break;
                    default:
                        validBitstring &= 0xFBFF7FFABFBEF4BEL;
                        break;
                }
            }


            if (nearbyMapInfos[28] == null || nearbyMapInfos[28].hasRuin() || nearbyMapInfos[28].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xFBAF7EBAB7BAB6B7L;
            } else {
                PaintType paint = nearbyMapInfos[28].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xFBAF7EBAB7BAB6B7L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x450814548454948L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFBFF7FFBBFBFF6BFL;
                        break;
                    default:
                        validBitstring &= 0xFFAFFEBEF7FABFF7L;
                        break;
                }
            }


            if (nearbyMapInfos[29] == null || nearbyMapInfos[29].hasRuin() || nearbyMapInfos[29].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xFBAF7EBAF7BAF7FFL;
            } else {
                PaintType paint = nearbyMapInfos[29].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xFBAF7EBAF7BAF7FFL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x450814508450800L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFBFFEBFF7FAFFFFL;
                        break;
                    default:
                        validBitstring &= 0xFBEF7FFAFFBFF7FFL;
                        break;
                }
            }


            if (nearbyMapInfos[30] == null || nearbyMapInfos[30].hasRuin() || nearbyMapInfos[30].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0x7FD7BD7D7B5D7BFFL;
            } else {
                PaintType paint = nearbyMapInfos[30].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0x7FD7BD7D7B5D7BFFL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x8028428284A28400L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0x7FFFFFFD7F5FFBFFL;
                        break;
                    default:
                        validBitstring &= 0xFFD7BD7FFBFD7FFFL;
                        break;
                }
            }


            if (nearbyMapInfos[31] == null || nearbyMapInfos[31].hasRuin() || nearbyMapInfos[31].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0x7FD7BD7D7B555B5BL;
            } else {
                PaintType paint = nearbyMapInfos[31].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0x7FD7BD7D7B555B5BL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x8028428284AAA4A4L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFD7FD7FFFF55FFBL;
                        break;
                    default:
                        validBitstring &= 0x7FFFBFFD7B5FFB5FL;
                        break;
                }
            }


            if (nearbyMapInfos[32] == null || nearbyMapInfos[32].hasRuin() || nearbyMapInfos[32].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0x7FD7BD7D7B554948L;
            } else {
                PaintType paint = nearbyMapInfos[32].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0x7FD7BD7D7B554948L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x8028428284AAB6B7L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0x7FFFBFFD7F5FED5EL;
                        break;
                    default:
                        validBitstring &= 0xFFD7FD7FFBF55BE9L;
                        break;
                }
            }


            if (nearbyMapInfos[33] == null || nearbyMapInfos[33].hasRuin() || nearbyMapInfos[33].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xFFFFFD7D7B450800L;
            } else {
                PaintType paint = nearbyMapInfos[33].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xFFFFFD7D7B450800L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x28284BAF7FFL;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFFFFD7FFBE51FE5L;
                        break;
                    default:
                        validBitstring &= 0xFFFFFFFD7F5FE81AL;
                        break;
                }
            }


            if (nearbyMapInfos[34] == null || nearbyMapInfos[34].hasRuin() || nearbyMapInfos[34].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xFFFFFFFFFE000000L;
            } else {
                PaintType paint = nearbyMapInfos[34].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xFFFFFFFFFE000000L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x1FFFFFFL;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFFFFFFFFE1FE01FL;
                        break;
                    default:
                        validBitstring &= 0xFFFFFFFFFFE01FE0L;
                        break;
                }
            }


            if (nearbyMapInfos[35] == null || nearbyMapInfos[35].hasRuin() || nearbyMapInfos[35].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xFFFFFAFAF6A28400L;
            } else {
                PaintType paint = nearbyMapInfos[35].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xFFFFFAFAF6A28400L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x505095D7BFFL;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFFFFAFFF7E29FE9L;
                        break;
                    default:
                        validBitstring &= 0xFFFFFFFAFEBFE416L;
                        break;
                }
            }


            if (nearbyMapInfos[36] == null || nearbyMapInfos[36].hasRuin() || nearbyMapInfos[36].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xFFAF7AFAF6AAA4A4L;
            } else {
                PaintType paint = nearbyMapInfos[36].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xFFAF7AFAF6AAA4A4L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x50850509555B5BL;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFFF7FFAFEBFECBEL;
                        break;
                    default:
                        validBitstring &= 0xFFAFFAFFF7EAB7E5L;
                        break;
                }
            }


            if (nearbyMapInfos[37] == null || nearbyMapInfos[37].hasRuin() || nearbyMapInfos[37].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xFFAF7AFAF6AAB6B7L;
            } else {
                PaintType paint = nearbyMapInfos[37].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xFFAF7AFAF6AAB6B7L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x50850509554948L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFAFFAFFFFEABFF7L;
                        break;
                    default:
                        validBitstring &= 0xFFFF7FFAF6BFF6BFL;
                        break;
                }
            }


            if (nearbyMapInfos[38] == null || nearbyMapInfos[38].hasRuin() || nearbyMapInfos[38].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xFFAF7AFAF6BAF7FFL;
            } else {
                PaintType paint = nearbyMapInfos[38].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xFFAF7AFAF6BAF7FFL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x50850509450800L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFFFFFFAFEBFF7FFL;
                        break;
                    default:
                        validBitstring &= 0xFFAF7AFFF7FAFFFFL;
                        break;
                }
            }


            if (nearbyMapInfos[39] == null || nearbyMapInfos[39].hasRuin() || nearbyMapInfos[39].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xF7D7B5FD7B7D7BFFL;
            } else {
                PaintType paint = nearbyMapInfos[39].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xF7D7B5FD7B7D7BFFL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x8284A0284828400L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFF7F5FFFBFD7FFFL;
                        break;
                    default:
                        validBitstring &= 0xF7DFBFFD7F7FFBFFL;
                        break;
                }
            }


            if (nearbyMapInfos[40] == null || nearbyMapInfos[40].hasRuin() || nearbyMapInfos[40].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xF7D7B5F57B757B5BL;
            } else {
                PaintType paint = nearbyMapInfos[40].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xF7D7B5F57B757B5BL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x8284A0A848A84A4L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xF7FFBFF77F7FFB5FL;
                        break;
                    default:
                        validBitstring &= 0xFFD7F5FDFBF57FFBL;
                        break;
                }
            }


            if (nearbyMapInfos[41] == null || nearbyMapInfos[41].hasRuin() || nearbyMapInfos[41].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xF7D7B5F56B756B48L;
            } else {
                PaintType paint = nearbyMapInfos[41].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xF7D7B5F56B756B48L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x8284A0A948A94B7L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFD7F5FFEBF77FE9L;
                        break;
                    default:
                        validBitstring &= 0xF7FFBFF57F7DEB5EL;
                        break;
                }
            }


            if (nearbyMapInfos[42] == null || nearbyMapInfos[42].hasRuin() || nearbyMapInfos[42].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xF7FFF5E56B656A00L;
            } else {
                PaintType paint = nearbyMapInfos[42].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xF7FFF5E56B656A00L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x8000A1A949A95FFL;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xF7FFFFE57F7FEA9EL;
                        break;
                    default:
                        validBitstring &= 0xFFFFF5FFEBE57F61L;
                        break;
                }
            }


            if (nearbyMapInfos[43] == null || nearbyMapInfos[43].hasRuin() || nearbyMapInfos[43].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xFFFFE7E7EE606200L;
            } else {
                PaintType paint = nearbyMapInfos[43].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xFFFFE7E7EE606200L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x1818119F9DFFL;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFFFE7FFEFE07FF1L;
                        break;
                    default:
                        validBitstring &= 0xFFFFFFE7FE7FE20EL;
                        break;
                }
            }


            if (nearbyMapInfos[44] == null || nearbyMapInfos[44].hasRuin() || nearbyMapInfos[44].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xEFFFEBE2E6E2E600L;
            } else {
                PaintType paint = nearbyMapInfos[44].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xEFFFEBE2E6E2E600L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x1000141D191D19FFL;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xEFFFFFE2FEFFE71EL;
                        break;
                    default:
                        validBitstring &= 0xFFFFEBFFE7E2FEE1L;
                        break;
                }
            }


            if (nearbyMapInfos[45] == null || nearbyMapInfos[45].hasRuin() || nearbyMapInfos[45].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xEFAF6BEAE6EAE6A4L;
            } else {
                PaintType paint = nearbyMapInfos[45].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xEFAF6BEAE6EAE6A4L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x105094151915195BL;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFAFEBFFE7EEFFE5L;
                        break;
                    default:
                        validBitstring &= 0xEFFF7FEAFEFBE6BEL;
                        break;
                }
            }


            if (nearbyMapInfos[46] == null || nearbyMapInfos[46].hasRuin() || nearbyMapInfos[46].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xEFAF6BEAF6EAF6B7L;
            } else {
                PaintType paint = nearbyMapInfos[46].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xEFAF6BEAF6EAF6B7L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x1050941509150948L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xEFFF7FEEFEFFF6BFL;
                        break;
                    default:
                        validBitstring &= 0xFFAFEBFBF7EAFFF7L;
                        break;
                }
            }


            if (nearbyMapInfos[47] == null || nearbyMapInfos[47].hasRuin() || nearbyMapInfos[47].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xEFAF6BFAF6FAF7FFL;
            } else {
                PaintType paint = nearbyMapInfos[47].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xEFAF6BFAF6FAF7FFL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x1050940509050800L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFEFEBFFF7FAFFFFL;
                        break;
                    default:
                        validBitstring &= 0xEFBF7FFAFEFFF7FFL;
                        break;
                }
            }


            if (nearbyMapInfos[48] == null || nearbyMapInfos[48].hasRuin() || nearbyMapInfos[48].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xF7DFB5FDFB7DFBFFL;
            } else {
                PaintType paint = nearbyMapInfos[48].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xF7DFB5FDFB7DFBFFL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x8204A0204820400L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xF7FFBFFDFF7FFBFFL;
                        break;
                    default:
                        validBitstring &= 0xFFDFF5FFFBFDFFFFL;
                        break;
                }
            }


            if (nearbyMapInfos[49] == null || nearbyMapInfos[49].hasRuin() || nearbyMapInfos[49].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xF75FB5F5FB75FB7BL;
            } else {
                PaintType paint = nearbyMapInfos[49].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xF75FB5F5FB75FB7BL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x8A04A0A048A0484L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFF5FF7FFFBF5FFFBL;
                        break;
                    default:
                        validBitstring &= 0xF7FFBDF5FF7FFB7FL;
                        break;
                }
            }


            if (nearbyMapInfos[50] == null || nearbyMapInfos[50].hasRuin() || nearbyMapInfos[50].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xF75EB5F5EB75EB6AL;
            } else {
                PaintType paint = nearbyMapInfos[50].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xF75EB5F5EB75EB6AL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x8A14A0A148A1495L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xF7FEBFF5FFFFEB7EL;
                        break;
                    default:
                        validBitstring &= 0xFF5FF5FFEB75FFEBL;
                        break;
                }
            }


            if (nearbyMapInfos[51] == null || nearbyMapInfos[51].hasRuin() || nearbyMapInfos[51].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xF67EF5E5EB65EA62L;
            } else {
                PaintType paint = nearbyMapInfos[51].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xF67EF5E5EB65EA62L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x9810A1A149A159DL;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFE7FF5FFEBEDFFE3L;
                        break;
                    default:
                        validBitstring &= 0xF7FEFFE5FF77EA7EL;
                        break;
                }
            }


            if (nearbyMapInfos[52] == null || nearbyMapInfos[52].hasRuin() || nearbyMapInfos[52].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xFE7EE7E7EE61E262L;
            } else {
                PaintType paint = nearbyMapInfos[52].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xFE7EE7E7EE61E262L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x1811818119E1D9DL;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFFEFFE7FE7FF27EL;
                        break;
                    default:
                        validBitstring &= 0xFE7FE7FFEFE1EFE3L;
                        break;
                }
            }


            if (nearbyMapInfos[53] == null || nearbyMapInfos[53].hasRuin() || nearbyMapInfos[53].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xEE7EEBE3E6E3E662L;
            } else {
                PaintType paint = nearbyMapInfos[53].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xEE7EEBE3E6E3E662L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x1181141C191C199DL;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFE7FEBFFE7F3FFE3L;
                        break;
                    default:
                        validBitstring &= 0xEFFEFFE3FEEFE67EL;
                        break;
                }
            }


            if (nearbyMapInfos[54] == null || nearbyMapInfos[54].hasRuin() || nearbyMapInfos[54].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xEEBE6BEBE6EBE6E6L;
            } else {
                PaintType paint = nearbyMapInfos[54].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xEEBE6BEBE6EBE6E6L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x1141941419141919L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xEFFE7FEBFFFFE6FEL;
                        break;
                    default:
                        validBitstring &= 0xFEBFEBFFE6EBFFE7L;
                        break;
                }
            }


            if (nearbyMapInfos[55] == null || nearbyMapInfos[55].hasRuin() || nearbyMapInfos[55].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xEEBF6BEBF6EBF6F7L;
            } else {
                PaintType paint = nearbyMapInfos[55].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xEEBF6BEBF6EBF6F7L;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x1140941409140908L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFEBFEFFFF7EBFFF7L;
                        break;
                    default:
                        validBitstring &= 0xEFFF7BEBFEFFF6FFL;
                        break;
                }
            }


            if (nearbyMapInfos[56] == null || nearbyMapInfos[56].hasRuin() || nearbyMapInfos[56].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xEFBF6BFBF6FBF7FFL;
            } else {
                PaintType paint = nearbyMapInfos[56].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xEFBF6BFBF6FBF7FFL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x1040940409040800L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xEFFF7FFBFEFFF7FFL;
                        break;
                    default:
                        validBitstring &= 0xFFBFEBFFF7FBFFFFL;
                        break;
                }
            }


            if (nearbyMapInfos[57] == null || nearbyMapInfos[57].hasRuin() || nearbyMapInfos[57].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xF75FF5F5FF75FF7FL;
            } else {
                PaintType paint = nearbyMapInfos[57].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xF75FF5F5FF75FF7FL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x8A00A0A008A0080L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFFFFFF5FF7FFF7FL;
                        break;
                    default:
                        validBitstring &= 0xF75FF5FFFFF5FFFFL;
                        break;
                }
            }


            if (nearbyMapInfos[58] == null || nearbyMapInfos[58].hasRuin() || nearbyMapInfos[58].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xF75EF5F5EF75EF6FL;
            } else {
                PaintType paint = nearbyMapInfos[58].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xF75EF5F5EF75EF6FL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x8A10A0A108A1090L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFF5FFDFFEFF5FFEFL;
                        break;
                    default:
                        validBitstring &= 0xF7FEF7F5FF7FEF7FL;
                        break;
                }
            }


            if (nearbyMapInfos[59] == null || nearbyMapInfos[59].hasRuin() || nearbyMapInfos[59].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xF67EF5E5EF65EE6FL;
            } else {
                PaintType paint = nearbyMapInfos[59].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xF67EF5E5EF65EE6FL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x9810A1A109A1190L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xF7FEFFEDFF7FEE7FL;
                        break;
                    default:
                        validBitstring &= 0xFE7FF5F7EFE5FFEFL;
                        break;
                }
            }


            if (nearbyMapInfos[60] == null || nearbyMapInfos[60].hasRuin() || nearbyMapInfos[60].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xFE7EE7E7EE61EE6FL;
            } else {
                PaintType paint = nearbyMapInfos[60].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xFE7EE7E7EE61EE6FL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x1811818119E1190L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFE7FE7FFFFE1FFEFL;
                        break;
                    default:
                        validBitstring &= 0xFFFEFFE7EE7FEE7FL;
                        break;
                }
            }


            if (nearbyMapInfos[61] == null || nearbyMapInfos[61].hasRuin() || nearbyMapInfos[61].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xEE7EEBE3EEE3EE6FL;
            } else {
                PaintType paint = nearbyMapInfos[61].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xEE7EEBE3EEE3EE6FL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x1181141C111C1190L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xEFFEFFF3FEFFEE7FL;
                        break;
                    default:
                        validBitstring &= 0xFE7FEBEFEFE3FFEFL;
                        break;
                }
            }


            if (nearbyMapInfos[62] == null || nearbyMapInfos[62].hasRuin() || nearbyMapInfos[62].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xEEBEEBEBEEEBEEEFL;
            } else {
                PaintType paint = nearbyMapInfos[62].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xEEBEEBEBEEEBEEEFL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x1141141411141110L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFEBFFBFFEFEBFFEFL;
                        break;
                    default:
                        validBitstring &= 0xEFFEEFEBFEFFEEFFL;
                        break;
                }
            }


            if (nearbyMapInfos[63] == null || nearbyMapInfos[63].hasRuin() || nearbyMapInfos[63].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xEEBFEBEBFEEBFEFFL;
            } else {
                PaintType paint = nearbyMapInfos[63].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xEEBFEBEBFEEBFEFFL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x1140141401140100L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFFFFFEBFEFFFEFFL;
                        break;
                    default:
                        validBitstring &= 0xEEBFEBFFFFEBFFFFL;
                        break;
                }
            }


            if (nearbyMapInfos[64] == null || nearbyMapInfos[64].hasRuin() || nearbyMapInfos[64].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xF77EF5F7EF77EFFFL;
            } else {
                PaintType paint = nearbyMapInfos[64].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xF77EF5F7EF77EFFFL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x8810A0810881000L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xF7FEFFF7FF7FEFFFL;
                        break;
                    default:
                        validBitstring &= 0xFF7FF5FFEFF7FFFFL;
                        break;
                }
            }


            if (nearbyMapInfos[65] == null || nearbyMapInfos[65].hasRuin() || nearbyMapInfos[65].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xF67EF5E7EF67EFFFL;
            } else {
                PaintType paint = nearbyMapInfos[65].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xF67EF5E7EF67EFFFL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x9810A1810981000L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFEFFF5FFEFE7FFFFL;
                        break;
                    default:
                        validBitstring &= 0xF77EFFE7FF7FEFFFL;
                        break;
                }
            }


            if (nearbyMapInfos[66] == null || nearbyMapInfos[66].hasRuin() || nearbyMapInfos[66].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xFE7EE7E7EE67EFFFL;
            } else {
                PaintType paint = nearbyMapInfos[66].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xFE7EE7E7EE67EFFFL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x181181811981000L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFFFFFFE7FE7FEFFFL;
                        break;
                    default:
                        validBitstring &= 0xFE7EE7FFEFE7FFFFL;
                        break;
                }
            }


            if (nearbyMapInfos[67] == null || nearbyMapInfos[67].hasRuin() || nearbyMapInfos[67].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xEE7EEBE7EEE7EFFFL;
            } else {
                PaintType paint = nearbyMapInfos[67].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xEE7EEBE7EEE7EFFFL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x1181141811181000L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xFF7FEBFFEFE7FFFFL;
                        break;
                    default:
                        validBitstring &= 0xEEFEFFE7FEFFEFFFL;
                        break;
                }
            }


            if (nearbyMapInfos[68] == null || nearbyMapInfos[68].hasRuin() || nearbyMapInfos[68].isWall()) {
                // if it's a ruin, we might actually want a bit bigger radius around it, but whatever
                validBitstring &= 0xEEFEEBEFEEEFEFFFL;
            } else {
                PaintType paint = nearbyMapInfos[68].getPaint();
                switch(paint) {
                    case PaintType.ENEMY_PRIMARY:
                    case PaintType.ENEMY_SECONDARY:
                        validBitstring &= 0xEEFEEBEFEEEFEFFFL;
                        break;
                    case PaintType.EMPTY:
                        unfinishedBitstring |= 0x1101141011101000L;
                        break;
                    case PaintType.ALLY_PRIMARY:
                        validBitstring &= 0xEFFEFFEFFEFFEFFFL;
                        break;
                    default:
                        validBitstring &= 0xFEFFEBFFEFEFFFFFL;
                        break;
                }
            }

